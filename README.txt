Easier way:

Do build from vlc site:
https://wiki.videolan.org/iOSCompile

then merge it with this repository (with overwriting) and build again (with -n flag to not update via network).

Harder way:

Pull this repo and resolve dependencies and then build. 